name: gsk job
#trigger point
on: 
  push:
    branches: [ "dryrun-aws" ]  
    paths-ignore:
      - "README.md"
      - "terraform.tfstate"

# creating jobs 
jobs:
  test-job:
    runs-on: ubuntu-latest
    steps: 
    #calling first github action to clone my git repo dryrun to runner machine
    - name: taking code from my repo
      uses: actions/checkout@v5
      with:
        ref: dryrun-aws

    - name: my first step to test github action
      run: |
           echo "Hello WOrld"
           git version
           whoami
           ls -a
           
  


